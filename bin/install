#!/usr/bin/env bash

set -x

cp ./bin/sw /usr/local/bin/sw

cmds=($(grep '  (' /usr/local/bin/sw | tr -d '(' | tr -d ')'))

for cmd in ${cmds[@]}
do
  if [[ ! -x /usr/local/bin/${cmd} ]]
  then ln -fs /usr/local/bin/sw /usr/local/bin/${cmd}
  fi
done

exit 0
