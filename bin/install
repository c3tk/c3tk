#!/usr/bin/env bash

cp ./bin/dtk /usr/local/bin/dtk

cmds=($(grep '  (' /usr/local/bin/dtk | tr -d '(' | tr -d ')'))

for cmd in ${cmds[@]}
do
  if [[ ! -x /usr/local/bin/${cmd} ]]
  then ln -fs /usr/local/bin/dtk /usr/local/bin/${cmd}
  fi
done

exit 0
