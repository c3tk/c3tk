#!/usr/bin/env bash

cp ./bin/c3tk /usr/local/bin/c3tk

cmds=($(grep '  (' ./bin/c3tk | tr -d '(' | tr -d ')'))

for cmd in ${cmds[@]}
do
  if [[ ! -x /usr/local/bin/${cmd} ]]
  then ln -fs /usr/local/bin/c3tk /usr/local/bin/${cmd}
  fi
done

exit 0
